[workspace]
name = "foo"
channels = ["https://prefix.dev/conda-forge"]
platforms = ["linux-64"]

[dependencies]
foo = { path = "." }

[package]
name = "foo"
version = "0.0.1"
description = "Fixture package — exists only so mise ci discovery has something to find"
license = "MIT"
authors = ["Greenroom Robotics"]

[package.build.backend]
name = "pixi-build-python"
version = "*"
